<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:Seevalocal.UI.ViewModels"
        xmlns:views="using:Seevalocal.UI.Views"
        xmlns:local="using:Seevalocal.UI"
        xmlns:converters="using:Seevalocal.UI.Converters"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="700"
        x:Class="Seevalocal.UI.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="{Binding TitleBarText}"
        Width="1200" Height="700"
        Background="#181825" Foreground="#CDD6F4">

  <Window.Styles>
    <!-- Sidebar button style -->
    <Style Selector="Button.navButton">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#BAC2E8"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Padding" Value="12,10"/>
      <Setter Property="Margin" Value="4,2"/>
      <Setter Property="CornerRadius" Value="6"/>
    </Style>
    <Style Selector="Button.navButton:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#313244"/>
    </Style>
    <Style Selector="Button.navButton.active">
      <Setter Property="Background" Value="#313244"/>
      <Setter Property="Foreground" Value="#89B4FA"/>
    </Style>
  </Window.Styles>

  <Grid ColumnDefinitions="200,*">
    <!-- Sidebar Navigation -->
    <Border Grid.Column="0" Background="#1E1E2E" Padding="8">
      <StackPanel Spacing="4">
        <TextBlock Text="Seevalocal" FontSize="20" FontWeight="Bold" 
                   Foreground="#89B4FA" Margin="0,8,0,16"/>
        
        <Button Classes="navButton"
                Classes.active="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Wizard}}"
                Command="{Binding NavigateToWizardCommand}">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="📝" FontSize="16"/>
            <TextBlock Text="Setup Wizard" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>

        <Button Classes="navButton"
                Classes.active="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.RunDashboard}}"
                Command="{Binding NavigateToDashboardCommand}">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="▶️" FontSize="16"/>
            <TextBlock Text="Run Dashboard" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>

        <Button Classes="navButton"
                Classes.active="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Results}}"
                Command="{Binding NavigateToResultsCommand}">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="📊" FontSize="16"/>
            <TextBlock Text="Results Viewer" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>

        <Button Classes="navButton"
                Classes.active="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Settings}}"
                Command="{Binding NavigateToSettingsCommand}">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="⚙️" FontSize="16"/>
            <TextBlock Text="Settings" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>

        <Separator Margin="0,16,0,8" Background="#45475A"/>
        
        <!-- Settings file stack -->
        <TextBlock Text="Settings Stack" FontSize="12" Foreground="#7F849C" Margin="0,8,0,4"/>
        <ItemsControl ItemsSource="{Binding SettingsLayers}" Margin="0,0,0,8">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,2">
                <CheckBox IsChecked="{Binding IsEnabled}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding DisplayName}" FontSize="11" Foreground="#BAC2E8" 
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <Button Content="+ Add Settings File"
                HorizontalAlignment="Stretch"
                Background="#313244" Foreground="#CDD6F4"
                Padding="8,6" CornerRadius="4"
                Command="{Binding AddSettingsFileCommand}"/>
      </StackPanel>
    </Border>

    <!-- Main Content Area -->
    <Border Grid.Column="1" Padding="16">
      <Grid RowDefinitions="*">
        <!-- Wizard View -->
        <views:WizardView Grid.Row="0" x:Name="WizardView"
                          IsVisible="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Wizard}}"/>

        <!-- Run Dashboard View -->
        <views:RunDashboardView Grid.Row="0" x:Name="RunDashboardView"
            IsVisible="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.RunDashboard}}"/>

        <!-- Results View -->
        <views:ResultsView Grid.Row="0" x:Name="ResultsView"
            IsVisible="{Binding CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Results}}"/>

        <!-- Settings View -->
        <views:SettingsView Grid.Row="0" x:Name="SettingsView"
            DataContext="{Binding SettingsViewModel}"
            IsVisible="{Binding $parent[Window].DataContext.CurrentView, Converter={x:Static converters:IsEqualConverter.Instance}, ConverterParameter={x:Static vm:AppView.Settings}}"/>
      </Grid>
    </Border>

    <!-- Toast Notification Overlay -->
    <Border Grid.Column="0" Grid.ColumnSpan="2" 
            HorizontalAlignment="Right" VerticalAlignment="Top" 
            Margin="0,20,20,0" MaxWidth="400">
      <ItemsControl ItemsSource="{Binding Toasts}" HorizontalAlignment="Right">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Vertical" Spacing="8" HorizontalAlignment="Right"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Background="#313244" CornerRadius="8" Padding="16" 
                    BorderThickness="1" BorderBrush="#45475A"
                    BoxShadow="0 4 12 2 #00000040">
              <StackPanel Orientation="Horizontal" Spacing="12">
                <!-- Icon based on toast kind -->
                <TextBlock FontSize="18">
                  <TextBlock.Text>
                    <Binding Path="Kind" Converter="{x:Static converters:ToastKindIconConverter.Instance}"/>
                  </TextBlock.Text>
                </TextBlock>
                <!-- Message -->
                <TextBlock Text="{Binding Message}" 
                           Foreground="#CDD6F4" 
                           FontSize="13"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="320"/>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Border>
  </Grid>
</Window>
